import { Box, Text } from "@chakra-ui/react";
import React from "react";
import Nav from "./Nav";

const Footer = () => {
  return (
    <Box>
      <Nav isFooter />
    </Box>
  );
};

export default Footer;
